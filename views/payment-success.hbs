<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Success</title>
    <link rel="stylesheet" href="/fa-5.15.1/all.min.css" />
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
    <meta http-equiv="refresh" content="15; URL=/ecomhome" />
</head>

<body>
    <style>/bootstrap-4.0.0-dist/css/bootstrap.min.css
        i.fas {
            font-size: min(50vw, 16em);
        }
    </style>
    {{>header}}
    <!-- d-none is not visible -->
    <div class="d-flex flex-column align-items-center">
        <i class="fas fa-ticket-alt py-4"></i>
        <h2>Payment Successful</h2>
        <p>Thank you for your purchase</p>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Product No</th>
                    <th scope="col">Customer ID</th>
                    <th scope="col">Quantity</th>
                    <th scope="col">Cost</th>
                    <th scope="col">Status</th>

                </tr>
            </thead>
            <tbody>
                {{#each response}}
                <tr>
                    <td>{{this._id}}</td>
                    <td>{{this.productno}}</td>
                    <td>{{this.customerid}}</td>
                    <td>{{this.quantity}}</td>
                    <td>{{this.cost}}</td>
                    <td>{{this.delivered}}</td>

                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
    <!-- d-flex is visible with addition decorations -->
    <div class="d-none flex-column align-items-center">
        <i class="fas fa-exclamation-triangle py-4"></i>
        <h2>Payment Failed</h2>
        <p>Contact our Office for further Query</p>
    </div>
</body>

</html>